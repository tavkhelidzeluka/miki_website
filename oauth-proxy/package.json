{
  "name": "miki-oauth-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub OAuth code-exchange proxy for Decap CMS, deployed to Cloudflare Workers.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
